{
  "name": "cbc-registration-tests",
  "version": "1.0.0",
  "description": "Test suite for CBC Registration System",
  "private": true,
  "scripts": {
    "test:email-validation": "jest email_validation.test.js",
    "test:email-validation:verbose": "jest email_validation.test.js --verbose"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
